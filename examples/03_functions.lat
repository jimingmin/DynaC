// Functions and returns
fn add(a, b) {
    return a + b;
}
print add(2, 3);

// Nested function and closure
fn makeCounter() {
    var n = 0;
    fn inc() {
        n = n + 1;
        return n;
    }
    return inc;
}
var counter = makeCounter();
print counter(); // 1
print counter(); // 2
